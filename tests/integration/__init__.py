"""Integration tests — synthetic networks with computable answers
(project plan §21).

Sanity checks where the answer is known by hand: small networks with
trivially solvable clearing vectors, balance-sheet identities with
constructed nodes, sectoral aggregates with known sub-totals.
"""
